#include <stdio.h>

int main()
{
  char str[256];

  printf("Enter a string: ");
  scanf("%s", str);

  int len = 0;

  while (str[len] != '\0')
  {
    len++;
  }

  printf("Length of the string is %d\n", len);
}


